import React, { ReactElement } from "react";
import { View } from "react-native";

import Screen from "../../components/screen/Screen";

function OrganizeScreen(): ReactElement {
  return (
    <Screen>
      <View></View>
    </Screen>
  );
}

export default OrganizeScreen;
