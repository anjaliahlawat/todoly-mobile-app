import React from "react";

const navigationRef = React.createRef<HTMLLinkElement>();

// const navigate = (name?: string, params?: unknown): void => {
//   if (!navigationRef.current) navigationRef.current.navigate(name, params);
// }

// export default {
//   navigate,
//   navigationRef,
// };
